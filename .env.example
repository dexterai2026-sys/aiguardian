# No environment variables are required yet.
# Phase 1 (shared rules, test corpus, TypeScript engine) runs entirely offline with no
# external services or secrets. Add variables here — and document them — as later phases
# introduce the backend, dashboard, and mobile/desktop apps.
