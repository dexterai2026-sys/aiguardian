import { describe, expect, it } from "vitest";
import { mask } from "./mask.js";

describe("mask", () => {
  it("is not implemented yet", () => {
    expect(() => mask("hello", [])).toThrow(/not implemented/);
  });
});
