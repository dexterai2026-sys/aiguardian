{
  "name": "@guardian/engine-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TypeScript detection engine (Tiers 1-2): PII, secrets, injection, and family content-flag detection. Consumed by the browser extension and dashboard.",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
