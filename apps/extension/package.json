{
  "name": "@guardian/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Guardian browser extension (Manifest V3): Chrome, Edge, Brave.",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "pretest": "vite build",
    "test": "vitest run && playwright test"
  },
  "dependencies": {
    "@guardian/engine-ts": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.3.0",
    "jsdom": "^30.1.1",
    "typescript": "^5.9.3",
    "vite": "^8.3.1",
    "vite-plugin-web-extension": "^4.5.1",
    "vitest": "^3.2.4"
  }
}
